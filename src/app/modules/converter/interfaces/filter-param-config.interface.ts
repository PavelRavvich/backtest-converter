import { FormGroup } from '@angular/forms';

// Config for create filter-param-list
export interface IFilterParamConfig {

    // Identification
    id: string;

    // Filter form
    form: FormGroup;

}
